# 🚀 Quick Test Login (Development)

## الحل السريع لتسجيل الدخول للتطوير

### استخدم رقم اختبار:

1. **رقم الجوال:** `0555555555`
2. **رمز التحقق:** `0000`
3. **اضغط تسجيل الدخول**

✅ **يعمل مباشرة بدون أي إعدادات!**

---

## أرقام اختبار أخرى

يمكنك استخدام أي رقم يبدأ بـ `555`:
- `0555555555` → رمز: `0000`
- `0555555556` → رمز: `0000`
- `0555555557` → رمز: `0000`

---

## بعد تسجيل الدخول

تحقق من أنك مسجل دخول:
```javascript
// في Console (F12)
const { data: { user } } = await supabase.auth.getUser();
console.log("✅ User ID:", user?.id);
```

إذا رأيت `User ID` موجود، أنت مسجل دخول بنجاح! ✅

---

## الآن جرب إنشاء طلب

بعد تسجيل الدخول، جرب إنشاء طلب جديد. يجب أن يعمل الآن!
