# إصلاح مشكلة OTP المنتهي الصلاحية للأرقام الاختبارية

## المشكلة

عند استخدام رقم اختبار مثل `555111111` أو `0555111111`، قد تظهر رسالة "رمز التحقق منتهي الصلاحية" حتى بعد إدخال الرمز `0000`.

## الحل المطبق

### 1. إضافة رقم `555111111` إلى config.toml

تم إضافة:
```toml
966555111111 = "0000"  # 555111111 (9 أرقام)
```

### 2. تحسين معالجة OTP المنتهي الصلاحية

الكود الآن:
1. ✅ يتعرف على الأرقام الاختبارية (0555, 0551, 0552, 555...)
2. ✅ عند انتهاء صلاحية OTP، يرسل OTP جديد تلقائياً
3. ✅ يحاول التحقق مرة أخرى بعد إرسال OTP الجديد

## كيفية الاستخدام

### الخطوة 1: إرسال OTP
1. أدخل الرقم: `555111111` أو `0555111111`
2. اضغط "إرسال رمز التحقق"
3. **انتظر قليلاً** (ثانية أو ثانيتين)

### الخطوة 2: إدخال الرمز
1. أدخل الرمز: `0000`
2. اضغط "تأكيد الدخول"
3. إذا ظهرت رسالة "منتهي الصلاحية":
   - الكود سيرسل OTP جديد تلقائياً
   - ثم يحاول التحقق مرة أخرى
   - يجب أن يعمل تلقائياً

## الأرقام الاختبارية المدعومة

- ✅ `0555111111` → `966551111111` → `0000`
- ✅ `0555222222` → `966552222222` → `0000`
- ✅ `555111111` → `966555111111` → `0000` (جديد)
- ✅ أي رقم يبدأ بـ `0555`, `0551`, `0552`, أو `555`

## استكشاف الأخطاء

إذا استمرت المشكلة:

1. **تحقق من Console Logs:**
   - ابحث عن `TEST PHONE` logs
   - تحقق من `verifyOtp` success/failure

2. **تحقق من config.toml:**
   - تأكد من وجود الرقم في `[auth.sms.test_otp]`
   - تأكد من تنسيق الرقم (966555111111)

3. **أعد إرسال OTP:**
   - اضغط "إعادة الإرسال" في الواجهة
   - ثم أدخل `0000` مرة أخرى

## ملاحظات

- ⏱️ **انتظر قليلاً** بعد إرسال OTP (1-2 ثانية)
- 🔄 الكود يحاول إعادة الإرسال تلقائياً عند انتهاء الصلاحية
- ✅ الرمز دائماً `0000` للأرقام الاختبارية
