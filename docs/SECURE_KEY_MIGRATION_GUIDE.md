# 🔐 دليل تغيير المفاتيح بأمان - Secure Key Migration Guide

دليل شامل لتغيير **جميع المفاتيح التجريبية** بمفاتيح إنتاج آمنة.

---

## ⚠️ أهمية تغيير المفاتيح التجريبية

المفاتيح التجريبية:
- ❌ قد تكون منتهية الصلاحية
- ❌ قد تكون محدودة الاستخدام
- ❌ قد تكون معرّضة للخطر
- ❌ قد تكون مرتبطة بحسابات تجريبية

**يجب تغييرها فوراً!** 🔒

---

## 📋 قائمة المفاتيح المطلوب تغييرها

### ✅ **ضرورية** (يجب تغييرها):
1. [ ] **Supabase** (`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`)
2. [ ] **Anthropic** (`VITE_ANTHROPIC_API_KEY`)
3. [ ] **OpenAI** (`VITE_OPENAI_API_KEY`)

### ⚠️ **مهمة** (يجب تغييرها):
4. [ ] **Google OAuth** (`VITE_GOOGLE_CLIENT_ID`)
5. [ ] **Google Maps** (`VITE_GOOGLE_MAPS_API_KEY`, `VITE_GOOGLE_MAPS_MAP_ID`)

### 🔧 **اختيارية لكن مُوصى بها**:
6. [ ] **Twilio** (`VITE_TWILIO_ACCOUNT_SID`, `VITE_TWILIO_AUTH_TOKEN`, `VITE_TWILIO_VERIFY_SERVICE_SID`)

---

## 🚀 خطة التنفيذ

### المرحلة 1: الحصول على المفاتيح الجديدة (30-60 دقيقة)

#### 1️⃣ Supabase (الأهم - 10 دقائق)

1. اذهب إلى: https://supabase.com
2. أنشئ مشروع جديد (أو استخدم مشروع إنتاج موجود)
3. انتظر حتى يتم إنشاء المشروع (2-3 دقائق)
4. اذهب إلى **Settings** → **API**
5. انسخ:
   - **Project URL** → `VITE_SUPABASE_URL`
   - **anon public key** → `VITE_SUPABASE_ANON_KEY`

**⚠️ مهم**: **لا تحذف المشروع القديم الآن** - سننقل البيانات لاحقاً إن أردت.

#### 2️⃣ Anthropic (5 دقائق)

1. اذهب إلى: https://console.anthropic.com/
2. سجّل دخول أو أنشئ حساب
3. اذهب إلى **API Keys**
4. اضغط **Create Key**
5. انسخ المفتاح (يبدأ بـ `sk-ant-api03-...`)
6. ضعه كـ `VITE_ANTHROPIC_API_KEY`

#### 3️⃣ OpenAI (5 دقائق)

1. اذهب إلى: https://platform.openai.com/
2. سجّل دخول أو أنشئ حساب
3. اذهب إلى **API Keys**
4. اضغط **Create new secret key**
5. انسخ المفتاح (يبدأ بـ `sk-proj-...` أو `sk-...`)
6. ضعه كـ `VITE_OPENAI_API_KEY`

#### 4️⃣ Google OAuth (15 دقيقة)

1. اذهب إلى: https://console.cloud.google.com/
2. أنشئ مشروع جديد (أو استخدم مشروع إنتاج)
3. اذهب إلى **APIs & Services** → **Credentials**
4. اضغط **Create Credentials** → **OAuth client ID**
5. إذا طلب إعداد OAuth consent screen:
   - اختر **External**
   - أدخل **App name** و **Support email**
   - احفظ
6. اختر **Web application**
7. أدخل **Name**
8. في **Authorized JavaScript origins**:
   - أضف: `http://localhost:3005` (للتطوير)
   - أضف: `https://yourdomain.com` (للإنتاج)
9. في **Authorized redirect URIs**:
   - أضف: `http://localhost:3005/auth/callback`
   - أضف: `https://yourdomain.com/auth/callback`
10. انسخ **Client ID** → `VITE_GOOGLE_CLIENT_ID`

#### 5️⃣ Google Maps (10 دقائق)

1. في **نفس Google Cloud Project** (من الخطوة السابقة)
2. اذهب إلى **APIs & Services** → **Library**
3. فعّل:
   - **Places API**
   - **Maps JavaScript API**
4. اذهب إلى **Credentials**
5. اضغط **Create Credentials** → **API Key**
6. (اختياري) حدّث Restrictions:
   - اختر **HTTP referrers**
   - أضف: `http://localhost:3005/*`
   - أضف: `https://yourdomain.com/*`
7. انسخ المفتاح → `VITE_GOOGLE_MAPS_API_KEY`
8. (اختياري) أنشئ **Map ID**:
   - اذهب إلى **Maps** → **Map Styles**
   - اضغط **Create Map ID**
   - انسخ **Map ID** → `VITE_GOOGLE_MAPS_MAP_ID`

#### 6️⃣ Twilio (20 دقيقة)

1. اذهب إلى: https://www.twilio.com/try-twilio
2. أنشئ حساب جديد (أو استخدم حساب إنتاج)
3. من Dashboard، انسخ:
   - **Account SID** → `VITE_TWILIO_ACCOUNT_SID`
   - **Auth Token** → `VITE_TWILIO_AUTH_TOKEN`
4. اذهب إلى **Verify** → **Services**
5. اضغط **Create new Verify Service**
6. أدخل **Service name**
7. انسخ **Service SID** (يبدأ بـ `VA...`) → `VITE_TWILIO_VERIFY_SERVICE_SID`

---

### المرحلة 2: تحديث ملف `.env` (5 دقائق)

1. افتح ملف `.env` (أو `.env.local`)
2. استبدل **جميع المفاتيح القديمة** بالمفاتيح الجديدة:

```env
# Supabase Configuration (مفاتيح جديدة!)
VITE_SUPABASE_URL=https://new-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Google OAuth Configuration (مفتاح جديد!)
VITE_GOOGLE_CLIENT_ID=123456789-new.apps.googleusercontent.com

# AI Configuration (مفاتيح جديدة!)
VITE_ANTHROPIC_API_KEY=sk-ant-api03-newxxxxx
VITE_OPENAI_API_KEY=sk-proj-newxxxxx

# Twilio Verify Configuration (مفاتيح جديدة!)
VITE_TWILIO_ACCOUNT_SID=ACnewxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
VITE_TWILIO_AUTH_TOKEN=new_auth_token_here
VITE_TWILIO_VERIFY_SERVICE_SID=VAnewxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Google Maps API (مفاتيح جديدة!)
VITE_GOOGLE_MAPS_API_KEY=AIzaSynewxxxxx
VITE_GOOGLE_MAPS_MAP_ID=new_map_id_here
```

3. **احفظ الملف**

---

### المرحلة 3: إعداد Supabase الجديد (30-60 دقيقة)

**⚠️ هذا الجزء الأهم!** Supabase الجديد يحتاج إعداد كامل.

#### ✅ **الخبر الجيد: يمكنني مساعدتك تلقائياً!**

**أخبرني عندما تنتهي من المرحلتين 1 و 2** وسأساعدك في:

1. ✅ تطبيق جميع الـ SQL migrations على Supabase الجديد
2. ✅ إنشاء جميع الجداول والـ policies
3. ✅ رفع Edge Functions
4. ✅ تفعيل Realtime
5. ✅ التحقق من أن كل شيء يعمل

**أو إذا أردت عملها يدوياً:**

راجع ملف: `docs/AFTER_CHANGING_ENV_KEYS.md` للتفاصيل.

---

### المرحلة 4: التحقق من كل شيء (10 دقائق)

1. **أعد تشغيل الـ dev server:**
   ```bash
   npm run dev
   ```

2. **تحقق من الاتصالات:**
   - افتح التطبيق: `http://localhost:3005`
   - تحقق من Console (F12) - لا يجب أن تكون هناك أخطاء
   - تأكد أن شريط التحذير في الأعلى لا يظهر

3. **اختبر الميزات:**
   - ✅ جرب إنشاء طلب جديد → يجب أن يُحفظ في Supabase
   - ✅ جرب تسجيل الدخول عبر Google → يجب أن يعمل
   - ✅ جرب البحث عن مدينة → يجب أن يعمل Google Maps
   - ✅ جرب إنشاء عرض → يجب أن يُحفظ
   - ✅ جرب إرسال رسالة → يجب أن تعمل

---

### المرحلة 5: حذف/تعطيل المفاتيح القديمة (5 دقائق)

**⚠️ بعد التأكد أن كل شيء يعمل:**

1. **Supabase القديم:**
   - يمكنك حذف المشروع القديم من Dashboard
   - أو تركه إذا أردت نسخ بيانات منه لاحقاً

2. **Anthropic/OpenAI:**
   - اذهب إلى Console وحذف المفاتيح القديمة
   - أو عطّلها

3. **Google Cloud:**
   - احذف OAuth Client ID القديم
   - احذف API Keys القديمة

4. **Twilio:**
   - احذف Verify Service القديم
   - أو عطّله

---

## 📝 قائمة التحقق النهائية

### ✅ قبل البدء:
- [ ] قرأت الدليل بالكامل
- [ ] أعددت وقتاً كافياً (1-2 ساعة)
- [ ] أنشأت حسابات إنتاج جديدة (إذا لزم الأمر)

### ✅ المرحلة 1: الحصول على المفاتيح:
- [ ] Supabase - مشروع جديد + مفاتيح
- [ ] Anthropic - مفتاح جديد
- [ ] OpenAI - مفتاح جديد
- [ ] Google OAuth - Client ID جديد + Redirect URIs محدثة
- [ ] Google Maps - API Key جديد + Map ID (اختياري)
- [ ] Twilio - Account SID + Auth Token + Verify Service SID

### ✅ المرحلة 2: تحديث `.env`:
- [ ] استبدلت جميع المفاتيح في `.env`
- [ ] احتفظت بنسخة احتياطية من المفاتيح القديمة (في مكان آمن!)

### ✅ المرحلة 3: إعداد Supabase:
- [ ] طبّقت SQL migrations (سأساعدك هنا!)
- [ ] رفعت Edge Functions
- [ ] فعّلت Realtime

### ✅ المرحلة 4: التحقق:
- [ ] التطبيق يعمل بدون أخطاء
- [ ] إنشاء الطلبات يعمل
- [ ] تسجيل الدخول عبر Google يعمل
- [ ] البحث عن المدن يعمل
- [ ] الرسائل تعمل

### ✅ المرحلة 5: التنظيف:
- [ ] حذفت/عطّلت المفاتيح القديمة
- [ ] تأكدت أن المفاتيح الجديدة فقط موجودة في `.env`

---

## 🆘 المساعدة

إذا واجهت أي مشاكل:

1. **تحقق من Console** (F12) - ابحث عن أخطاء
2. **تحقق من `.env`** - تأكد أن المفاتيح صحيحة
3. **تحقق من Supabase Dashboard** - تأكد أن الجداول موجودة
4. **أخبرني!** - سأساعدك عبر MCP Supabase

---

## 🎯 ملاحظات أمنية مهمة

### ⚠️ **لا تفعل هذا أبداً:**
- ❌ لا ترفع ملف `.env` إلى Git
- ❌ لا تشارك المفاتيح مع أحد
- ❌ لا تستخدم مفاتيح إنتاج في التطوير (يمكنك استخدام مفاتيح منفصلة)
- ❌ لا تترك المفاتيح القديمة مفعّلة بعد التأكد من عمل الجديدة

### ✅ **افعل هذا:**
- ✅ استخدم `.env.local` للتطوير (مضاف في `.gitignore`)
- ✅ استخدم Environment Variables في Vercel/Netlify للإنتاج
- ✅ احفظ المفاتيح في مكان آمن (مثل 1Password, Bitwarden)
- ✅ راجع استخدام المفاتيح بانتظام

---

## 🚀 جاهز للبدء!

**عندما تنتهي من المرحلتين 1 و 2 (الحصول على المفاتيح وتحديث `.env`)، أخبرني** وسأساعدك في إعداد Supabase الجديد تلقائياً! 😊

**الوقت المتوقع:** 1-2 ساعة (مع مساعدتي سيكون أسرع!)

---

**آخر تحديث**: ديسمبر 2024

**💡 نصيحة**: ابدأ بـ Supabase أولاً (الأهم)، ثم Anthropic/OpenAI، ثم البقية.
