# 🔧 حل سريع: رمز التحقق ما ينرسل

## 🔍 التشخيص السريع

### الخطوة 1: افتح Console في المتصفح (F12)
ابحث عن هذه الرسائل:
- `🔍 [DEBUG] Supabase URL:`
- `❌ Supabase OTP Error:`
- `🔍 [DEBUG] Full error object:`

### الخطوة 2: تحقق من نوع Supabase

**إذا كان URL يحتوي على `localhost` أو `127.0.0.1`:**
- أنت تستخدم **Supabase Local**
- تحتاج إلى: `supabase start`
- المتغيرات المطلوبة في `.env.local`:
  ```
  TWILIO_ACCOUNT_SID=...
  TWILIO_AUTH_TOKEN=...
  TWILIO_MESSAGE_SERVICE_SID=...
  ```

**إذا كان URL يحتوي على `supabase.co`:**
- أنت تستخدم **Supabase Production**
- تحتاج إلى إعداد Twilio في Dashboard:
  1. اذهب إلى: https://app.supabase.com
  2. اختر مشروعك
  3. **Authentication** → **Providers** → **Phone** → **Twilio**
  4. أضف:
     - Account SID
     - Auth Token
     - Message Service SID

---

## ✅ الحلول السريعة

### الحل 1: للتطوير (استخدم أرقام اختبار)
```
الرقم: 0555555555
الرمز: 0000
```
هذا يعمل بدون أي إعدادات Twilio!

### الحل 2: Supabase Local
```bash
# 1. تأكد من وجود المتغيرات في .env.local
TWILIO_ACCOUNT_SID=...
TWILIO_AUTH_TOKEN=...
TWILIO_MESSAGE_SERVICE_SID=...

# 2. شغّل Supabase
supabase start

# 3. أعد تشغيل dev server
npm run dev
```

### الحل 3: Supabase Production
1. اذهب إلى Supabase Dashboard
2. **Authentication** → **Providers** → **Phone** → **Twilio**
3. أضف بيانات Twilio
4. احفظ

---

## 🐛 الأخطاء الشائعة

### خطأ: "provider" أو "Twilio"
**السبب**: Twilio غير مُعد في Supabase

**الحل**:
- للتطوير: استخدم رقم يبدأ بـ `555` مع الرمز `0000`
- للإنتاج: أعد إعداد Twilio في Dashboard

### خطأ: "Invalid phone number"
**السبب**: تنسيق الرقم غير صحيح

**الحل**: استخدم رقم سعودي (9 أو 10 أرقام)
- `501234567` ✅
- `0501234567` ✅

### خطأ: "Rate limit"
**السبب**: طلبات كثيرة جداً

**الحل**: انتظر 5 ثواني ثم حاول مرة أخرى

---

## 🔍 للتحقق من المشكلة

1. افتح Console (F12)
2. ابحث عن `[DEBUG]` messages
3. انسخ الخطأ الكامل
4. راجع الأخطاء الشائعة أعلاه
