# 🔑 كيفية إنشاء Supabase Anon Key جديد

## 📍 الخطوات التفصيلية

### الخطوة 1: الدخول إلى Supabase Dashboard

1. اذهب إلى: **https://supabase.com/dashboard**
2. سجل الدخول بحسابك
3. اختر **المشروع** الذي تريد إنشاء anon key له

---

### الخطوة 2: الوصول إلى صفحة API Settings

1. من القائمة الجانبية، اضغط على **Settings** (الإعدادات)
2. من القائمة الفرعية، اضغط على **API**

ستظهر لك صفحة تحتوي على:
- **Project URL** (رابط المشروع)
- **Project API keys** (مفاتيح API للمشروع)

---

### الخطوة 3: العثور على Anon Key الحالي

في قسم **Project API keys**، ستجد مفاتيح مختلفة:

- **`anon` `public`** - هذا هو المفتاح الذي تحتاجه (VITE_SUPABASE_ANON_KEY)
- **`service_role` `secret`** - هذا مفتاح سري لا تستخدمه في الكود

**⚠️ ملاحظة**: المفتاح `anon` `public` هو المفتاح الآمن للاستخدام في الكود لأنه مقيد بـ Row Level Security (RLS).

---

### الخطوة 4: تعطيل المفتاح القديم (إن أردت)

إذا كان لديك مفتاح قديم مكشوف وتريد تعطيله:

1. ابحث عن المفتاح القديم في القائمة
2. اضغط على **Revoke** (تعطيل) بجانب المفتاح
3. سيتم تعطيل المفتاح فوراً

**⚠️ تحذير**: بعد تعطيل المفتاح، سيتم إيقاف جميع التطبيقات التي تستخدمه. تأكد من تحديث المفتاح في جميع الأماكن قبل التعطيل.

---

### الخطوة 5: إنشاء Anon Key جديد

**ملاحظة مهمة**: Supabase لا تسمح بإنشاء anon key جديد مباشرة. المفتاح `anon` `public` هو مفتاح ثابت لكل مشروع.

#### الخيار 1: استخدام المفتاح الحالي (إن لم يكن مكشوفاً)

إذا كان المفتاح الحالي لم يتم كشفه بعد، يمكنك:
1. انسخ المفتاح `anon` `public` الحالي
2. استخدمه في ملف `.env`

#### الخيار 2: إنشاء مشروع Supabase جديد (إن كان المفتاح مكشوفاً)

إذا كان المفتاح القديم مكشوفاً وتريد بداية جديدة:

1. اذهب إلى: **https://supabase.com/dashboard**
2. اضغط **New Project** (مشروع جديد)
3. املأ المعلومات:
   - **Name**: اسم المشروع
   - **Database Password**: كلمة مرور قاعدة البيانات
   - **Region**: المنطقة الجغرافية
4. اضغط **Create new project**
5. انتظر حتى يتم إنشاء المشروع (قد يستغرق دقيقة أو دقيقتين)
6. بعد الإنشاء، اذهب إلى **Settings** → **API**
7. انسخ:
   - **Project URL** → `VITE_SUPABASE_URL`
   - **`anon` `public`** key → `VITE_SUPABASE_ANON_KEY`

---

### الخطوة 6: نسخ المفاتيح

بعد الوصول إلى صفحة **Settings** → **API**:

1. **Project URL**:
   - سيبدو مثل: `https://xxxxxxxxxxxxx.supabase.co`
   - انسخه بالكامل
   - ضعه في `.env` كـ `VITE_SUPABASE_URL`

2. **`anon` `public` key**:
   - سيبدأ بـ `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
   - انسخه بالكامل (طويل جداً)
   - ضعه في `.env` كـ `VITE_SUPABASE_ANON_KEY`

---

### الخطوة 7: تحديث ملف .env

افتح ملف `.env` واستبدل المفاتيح القديمة:

```env
# Supabase
VITE_SUPABASE_URL=https://your-new-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlvdXItcHJvamVjdC1pZCIsInJvbGUiOiJhbm9uIiwiaWF0IjoxNjY0MzMxMjcsImV4cCI6MjA4MjAwOTEyN30.YOUR_NEW_KEY_HERE
```

---

### الخطوة 8: إعداد قاعدة البيانات الجديدة (إن أنشأت مشروع جديد)

إذا أنشأت مشروع Supabase جديد، يجب إعداد الجداول:

1. اذهب إلى **SQL Editor** في Supabase Dashboard
2. قم بتشغيل استعلامات SQL لإنشاء الجداول (راجع `SETUP.md` أو ملفات SQL في المشروع)

---

## 🔄 إذا كنت تستخدم مشروع Supabase موجود

### تحديث المفتاح في التطبيق فقط

إذا كان لديك مشروع Supabase موجود والمفتاح الحالي لم يتم كشفه:

1. اذهب إلى **Settings** → **API**
2. انسخ المفتاح `anon` `public` الحالي
3. ضعه في ملف `.env`

**⚠️ مهم**: لا يمكن إنشاء anon key جديد لمشروع موجود. المفتاح ثابت لكل مشروع.

---

## 🆘 في حالة الطوارئ (المفتاح مكشوف)

إذا كان المفتاح مكشوفاً ومستخدم بشكل خاطئ:

### الخيار 1: إنشاء مشروع جديد (موصى به)

1. أنشئ مشروع Supabase جديد
2. انسخ البيانات من المشروع القديم (إن أمكن)
3. استخدم المفاتيح الجديدة

### الخيار 2: مراجعة Row Level Security (RLS)

1. تأكد من تفعيل RLS على جميع الجداول
2. راجع السياسات الأمنية (Policies)
3. تأكد من أن المفتاح `anon` لا يمكنه الوصول إلى بيانات حساسة

---

## 📝 مثال على ملف .env

```env
# Supabase Configuration
VITE_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTY2NDMzMTI3LCJleHAiOjIwODIwMDkxMjd9.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

## ✅ Checklist

- [ ] دخلت إلى Supabase Dashboard
- [ ] ذهبت إلى Settings → API
- [ ] نسخت Project URL
- [ ] نسخت `anon` `public` key
- [ ] حدثت ملف `.env` بالمفاتيح الجديدة
- [ ] أعدت تشغيل المشروع
- [ ] اختبرت الاتصال بـ Supabase

---

## 🔗 روابط مفيدة

- [Supabase Dashboard](https://supabase.com/dashboard)
- [Supabase Documentation - API Keys](https://supabase.com/docs/guides/api/api-keys)
- [Supabase Documentation - Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)

---

## 💡 نصائح أمنية

1. **لا ترفع `.env` إلى Git** - تأكد من وجوده في `.gitignore`
2. **استخدم RLS** - تأكد من تفعيل Row Level Security على جميع الجداول
3. **راجع Policies** - تأكد من أن سياسات الأمان صحيحة
4. **راقب الاستخدام** - راجع استخدام API في Supabase Dashboard بانتظام

---

**ملاحظة**: المفتاح `anon` `public` آمن للاستخدام في الكود لأنه مقيد بـ RLS. المفتاح `service_role` `secret` يجب ألا يُستخدم أبداً في الكود لأنه يمنح صلاحيات كاملة.
