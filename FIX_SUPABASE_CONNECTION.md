# 🔧 إصلاح مشكلة الاتصال مع Supabase

## 🚨 المشكلة

الطلبات لا يمكن إنشاؤها بسبب مشكلة في الاتصال مع Supabase في المشروع الجديد (`gfjtyfwwbpjbwafbnfcc`).

---

## ✅ الحل (خطوة بخطوة)

### الخطوة 1: التحقق من حالة المشروع

1. اذهب إلى: https://app.supabase.com/project/gfjtyfwwbpjbwafbnfcc
2. تحقق من أن المشروع **نشط** وليس متوقف (paused)
3. إذا كان متوقفاً، فعّله

### الخطوة 2: التحقق من وجود جدول `requests`

1. في Supabase Dashboard → **Database** → **Tables**
2. تأكد من وجود جدول `requests`
3. إذا لم يكن موجوداً، يجب إنشاؤه أولاً

### الخطوة 3: إصلاح RLS Policies

1. اذهب إلى: **SQL Editor** في Supabase Dashboard
2. انسخ محتوى ملف: `supabase/FIX_REQUESTS_RLS_FOR_NEW_PROJECT.sql`
3. الصقه في SQL Editor
4. اضغط **Run** أو **Execute**

هذا سينشئ Policies التالية:
- ✅ **SELECT**: الجميع يمكنهم قراءة الطلبات العامة
- ✅ **INSERT**: المستخدمون المسجلون يمكنهم إنشاء طلبات
- ✅ **UPDATE**: المستخدمون يمكنهم تحديث طلباتهم
- ✅ **DELETE**: المستخدمون يمكنهم حذف طلباتهم

### الخطوة 4: التحقق من جدول `profiles`

إذا كان المستخدم غير مسجل (guest)، قد تحتاج إلى:
1. التحقق من RLS policies لجدول `profiles`
2. تطبيق: `supabase/FIX_PROFILES_RLS_NOW.sql`

---

## 🔍 التحقق من المشكلة

### في Console (F12)

ابحث عن:
- `❌ Supabase Insert Error`
- `permission` أو `policy` أو `RLS`
- `network` أو `fetch` أو `connection`

### في Supabase Dashboard

1. **Logs** → **Postgres Logs**
2. ابحث عن أخطاء عند محاولة إنشاء طلب
3. تحقق من رسائل الخطأ

---

## 📋 Checklist

قبل الاختبار، تأكد من:

- [ ] المشروع نشط (ليس paused)
- [ ] جدول `requests` موجود
- [ ] RLS Policies مطبقة على `requests`
- [ ] RLS Policies مطبقة على `profiles` (إذا كان المستخدم مسجل)
- [ ] تم حفظ التغييرات

---

## 🧪 اختبار

بعد تطبيق RLS Policies:

1. أعد تحميل الصفحة (F5)
2. جرب إنشاء طلب جديد
3. يجب أن يعمل بدون أخطاء

---

## 🆘 إذا استمرت المشكلة

شارك معي:
1. رسالة الخطأ من Console (F12)
2. رسالة الخطأ من Supabase Logs
3. نتيجة تطبيق SQL في SQL Editor
