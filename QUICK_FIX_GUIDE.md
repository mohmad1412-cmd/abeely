# 🔧 Quick Fix Guide - author_id is NULL

## المشكلة
`auth.uid()` يرجع `null` في Supabase SQL Editor لأن المستخدم غير مسجل دخول في Dashboard.

## الحل السريع

### الخطوة 1: احصل على User ID من Console

افتح Console (F12) في المتصفح وانسخ والصق:

```javascript
const { data: { user } } = await supabase.auth.getUser();
console.log("User ID:", user?.id);
```

**انسخ `User ID` الذي يظهر (يجب أن يكون UUID مثل: `123e4567-e89b-12d3-a456-426614174000`)**

---

### الخطوة 2: استخدم User ID في SQL

1. افتح `TEST_REQUEST_INSERT_WITH_USER_ID.sql`
2. استبدل `'PASTE_USER_ID_HERE'` بـ `user.id` من Console (مع الأقواس!)
3. مثال:
   ```sql
   '123e4567-e89b-12d3-a456-426614174000'::uuid
   ```
4. شغّل SQL

---

## إذا كان User ID = null في Console

**المشكلة: المستخدم غير مسجل دخول في التطبيق!**

### الحل:
1. سجل خروج من التطبيق
2. سجل دخول مرة أخرى
3. افتح Console وتحقق من `user.id` مرة أخرى
4. إذا كان موجوداً، استخدمه في SQL

---

## بعد تشغيل SQL

### إذا نجح INSERT:
✅ **RLS Policies تعمل بشكل صحيح!**
- المشكلة في الكود (author_id لا يتم تعيينه في التطبيق)
- راجع `App.tsx` السطر 3182-3218

### إذا فشل INSERT مع خطأ "permission" أو "policy":
❌ **المشكلة في RLS Policies**
- شغّل `FIX_ALL_RLS_NOW.sql` مرة أخرى في Supabase Dashboard

---

## الخطوات التالية

1. ✅ احصل على `user.id` من Console
2. ✅ استخدمه في `TEST_REQUEST_INSERT_WITH_USER_ID.sql`
3. ✅ شغّل SQL وأخبرني بالنتيجة
