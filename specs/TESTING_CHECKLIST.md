# قائمة اختبار نظام قبول العروض وإكمال الطلبات

## ✅ التحقق من الكود

### 1. التحقق من التكامل
- [x] دالة `acceptOffer` موجودة ومحسّنة في `services/requestsService.ts`
- [x] دالة `completeRequest` موجودة في `services/requestsService.ts`
- [x] دالة `sendPushNotificationForRequestCompleted` موجودة
- [x] Handler `handleCompleteRequest` موجود في `App.tsx`
- [x] `onCompleteRequest` متصل مع `RequestDetail` component
- [x] زر "تم إكمال الطلب" موجود في `RequestDetail.tsx`
- [x] البناء (build) نجح بدون أخطاء

### 2. التحقق من الصلاحيات
- [x] `acceptOffer` تتحقق من أن المستخدم هو صاحب الطلب
- [x] `completeRequest` تتحقق من أن المستخدم هو صاحب الطلب أو مقدم الخدمة المعتمد
- [x] التحقق من حالة الطلب قبل القبول/الإكمال

### 3. التحقق من معالجة الأخطاء
- [x] رسائل خطأ واضحة بالعربية
- [x] معالجة جميع الحالات الاستثنائية
- [x] Logging شامل للأخطاء

---

## 🧪 اختبارات يدوية

### اختبار 1: قبول عرض (Happy Path)

**الخطوات:**
1. تسجيل الدخول كصاحب طلب
2. إنشاء طلب جديد
3. انتظار وصول عرض (أو إنشاء عرض تجريبي)
4. فتح صفحة تفاصيل الطلب
5. النقر على زر "قبول العرض"

**النتائج المتوقعة:**
- [ ] رسالة نجاح تظهر
- [ ] حالة الطلب تتغير إلى "assigned"
- [ ] حالة العرض المقبول = "accepted"
- [ ] العروض الأخرى = "rejected"
- [ ] زر "تم إكمال الطلب" يظهر
- [ ] إشعار يصل لمقدم الخدمة

**النتيجة:** ⬜ نجح / ⬜ فشل

---

### اختبار 2: إكمال الطلب (صاحب الطلب)

**الخطوات:**
1. بعد قبول عرض (الاختبار 1)
2. النقر على زر "تم إكمال الطلب"
3. تأكيد الإكمال

**النتائج المتوقعة:**
- [ ] رسالة نجاح تظهر
- [ ] حالة الطلب تتغير إلى "completed"
- [ ] الزر يختفي بعد الإكمال
- [ ] إشعار يصل لصاحب الطلب
- [ ] إشعار يصل لمقدم الخدمة

**النتيجة:** ⬜ نجح / ⬜ فشل

---

### اختبار 3: إكمال الطلب (مقدم الخدمة)

**الخطوات:**
1. تسجيل الدخول كمقدم خدمة
2. فتح طلب تم قبول عرضه (status = "assigned")
3. النقر على زر "تم إكمال الطلب"

**النتائج المتوقعة:**
- [ ] رسالة نجاح تظهر
- [ ] حالة الطلب تتغير إلى "completed"
- [ ] إشعارات تصل للطرفين

**النتيجة:** ⬜ نجح / ⬜ فشل

---

### اختبار 4: محاولة قبول عرض بدون صلاحيات

**الخطوات:**
1. تسجيل الدخول كمستخدم عادي (ليس صاحب الطلب)
2. محاولة قبول عرض على طلب ليس له

**النتائج المتوقعة:**
- [ ] رسالة خطأ: "غير مصرح لك بقبول هذا العرض"
- [ ] لا يتم تحديث حالة العرض
- [ ] لا يتم تحديث حالة الطلب

**النتيجة:** ⬜ نجح / ⬜ فشل

---

### اختبار 5: محاولة إكمال طلب غير معين

**الخطوات:**
1. محاولة إكمال طلب في حالة "active" (لم يتم قبول عرض بعد)

**النتائج المتوقعة:**
- [ ] رسالة خطأ: "لا يمكن إكمال الطلب إلا بعد قبول عرض"
- [ ] لا يتم تحديث حالة الطلب

**النتيجة:** ⬜ نجح / ⬜ فشل

---

### اختبار 6: محاولة إكمال طلب بدون صلاحيات

**الخطوات:**
1. تسجيل الدخول كمستخدم عادي (ليس صاحب الطلب ولا مقدم الخدمة المعتمد)
2. محاولة إكمال طلب

**النتائج المتوقعة:**
- [ ] رسالة خطأ: "غير مصرح لك بإكمال هذا الطلب"
- [ ] لا يتم تحديث حالة الطلب

**النتيجة:** ⬜ نجح / ⬜ فشل

---

### اختبار 7: قبول عرض على طلب مكتمل

**الخطوات:**
1. محاولة قبول عرض على طلب في حالة "completed"

**النتائج المتوقعة:**
- [ ] رسالة خطأ: "لا يمكن قبول عرض على طلب مكتمل أو مؤرشف"
- [ ] لا يتم تحديث حالة العرض

**النتيجة:** ⬜ نجح / ⬜ فشل

---

### اختبار 8: قبول عرض مرفوض مسبقاً

**الخطوات:**
1. محاولة قبول عرض في حالة "rejected"

**النتائج المتوقعة:**
- [ ] رسالة خطأ: "هذا العرض تم قبوله أو رفضه مسبقاً"
- [ ] لا يتم تحديث حالة العرض

**النتيجة:** ⬜ نجح / ⬜ فشل

---

## 🔍 التحقق من Console

### أثناء الاختبار، تحقق من:
- [ ] لا توجد أخطاء في Console
- [ ] رسائل Log واضحة (✅ تم قبول العرض، ✅ تم إكمال الطلب)
- [ ] لا توجد تحذيرات (warnings)

---

## 📊 التحقق من قاعدة البيانات

### بعد كل اختبار، تحقق من Supabase:

#### بعد قبول عرض:
```sql
SELECT id, status, accepted_offer_id 
FROM requests 
WHERE id = 'request-id';
-- يجب أن يكون status = 'assigned'

SELECT id, status 
FROM offers 
WHERE request_id = 'request-id';
-- العرض المقبول يجب أن يكون status = 'accepted'
-- العروض الأخرى يجب أن تكون status = 'rejected'
```

#### بعد إكمال الطلب:
```sql
SELECT id, status 
FROM requests 
WHERE id = 'request-id';
-- يجب أن يكون status = 'completed'
```

---

## 🐛 المشاكل المكتشفة

### مشكلة 1: [إن وجدت]
**الوصف:**  
**الأولوية:** عالية / متوسطة / منخفضة  
**الحالة:** مفتوحة / قيد الإصلاح / تم الإصلاح

---

## ✅ الخلاصة

**تاريخ الاختبار:** ___________  
**المختبر:** ___________  

**النتيجة الإجمالية:**
- [ ] جميع الاختبارات نجحت ✅
- [ ] بعض الاختبارات فشلت ⚠️
- [ ] معظم الاختبارات فشلت ❌

**ملاحظات نهائية:**

---

**آخر تحديث:** 2025-01-27
