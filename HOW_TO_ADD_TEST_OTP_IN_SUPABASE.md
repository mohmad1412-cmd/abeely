# كيفية إضافة Test OTP في Supabase Dashboard

## ⚠️ ملاحظة مهمة

**`test_otp` في `config.toml` يعمل فقط في Local Development (Supabase CLI).**

في **Production (Supabase Cloud)**، قد لا يكون هناك خيار مباشر لإضافة `test_otp` في Dashboard. 

## الطرق المتاحة

### الطريقة 1: استخدام Supabase Dashboard (إن كان متاحاً)

1. اذهب إلى [Supabase Dashboard](https://app.supabase.com)
2. اختر مشروعك
3. اذهب إلى **Authentication** → **Settings**
4. ابحث عن قسم **Phone** أو **SMS**
5. ابحث عن **Test Phone Numbers** أو **Test OTP**
6. أضف الأرقام بالصيغة:
   ```
   +966551111111=0000
   +966552222222=0000
   ```

**⚠️ ملاحظة:** هذا الخيار قد لا يكون متاحاً في جميع المشاريع أو الخطط.

### الطريقة 2: استخدام Environment Variables (مُوصى به)

يمكن إضافة `test_otp` عبر Environment Variables في Supabase Dashboard:

1. اذهب إلى **Project Settings** → **API**
2. ابحث عن **Environment Variables** أو **Secrets**
3. أضف:
   ```
   SUPABASE_AUTH_SMS_TEST_OTP_966551111111=0000
   SUPABASE_AUTH_SMS_TEST_OTP_966552222222=0000
   ```

**⚠️ ملاحظة:** قد لا تعمل هذه الطريقة أيضاً، لأن Supabase قد لا يدعم test_otp في Production.

### الطريقة 3: استخدام الكود المحدث (الأفضل) ✅

تم تحديث الكود ليعمل **بدون الحاجة لـ test_otp في Production**. 

الكود الآن:
- ✅ يتعرف على الأرقام الاختبارية (0555...)
- ✅ يستخدم الرمز `0000` تلقائياً
- ✅ يعمل في Local Development (مع config.toml)
- ✅ يعمل في Production (بدون test_otp)

## الحل المطبق في الكود

الكود الآن يتعامل مع الأرقام الاختبارية بشكل ذكي:

1. **في Local Development:**
   - يستخدم `test_otp` من `config.toml`
   - يعمل مباشرة مع الرمز `0000`

2. **في Production:**
   - يتعرف على الأرقام التي تبدأ بـ `0555`
   - يحاول استخدام `verifyOtp` مباشرة
   - إذا فشل، يعيد إرسال OTP ثم يحاول مرة أخرى

## كيفية الاستخدام

### للأرقام الاختبارية:

1. **أدخل الرقم:** `0555111111` أو `0555222222`
2. **أدخل الرمز:** `0000`
3. **يجب أن يعمل تلقائياً!**

### إذا لم يعمل:

1. **تحقق من Console Logs:**
   - ابحث عن `TEST PHONE` logs
   - تحقق من الأخطاء

2. **في Local Development:**
   - تأكد من تشغيل `supabase start`
   - تأكد من وجود الرقم في `config.toml`

3. **في Production:**
   - الكود يعمل بدون test_otp
   - إذا استمرت المشكلة، استخدم رقم جوال حقيقي

## ملخص

- ✅ **Local Development:** استخدم `config.toml` (يعمل تلقائياً)
- ✅ **Production:** الكود يعمل بدون test_otp
- ✅ **الأرقام الاختبارية:** `0555111111`, `0555222222` مع الرمز `0000`

**لا حاجة لإضافة test_otp في Supabase Dashboard!** 🎉
