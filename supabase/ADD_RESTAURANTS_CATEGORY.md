# إضافة تصنيف "مطاعم" إلى قاعدة البيانات

## 📋 ملخص
تم إضافة تصنيف جديد "مطاعم" للطلبات المتعلقة بالمطاعم وخدمات المطاعم.

## ✅ التغييرات التي تمت

### 1. الكود (Frontend & Edge Functions)
- ✅ تمت إضافة التصنيف في `data.ts`
- ✅ تمت إضافة التصنيف في `supabase/functions/ai-chat/index.ts`
- ✅ تمت إضافة الألوان في `utils/categoryColors.ts`
- ✅ تم تحديث الـ system prompt ليشمل "مطاعم"

### 2. قاعدة البيانات (Database)
- ✅ تم إنشاء migration SQL في `supabase/migrations/add_restaurants_category.sql`

## 🚀 كيفية التطبيق

### خطوة 1: تطبيق Migration على قاعدة البيانات المحلية
```bash
# إذا كنت تستخدم Supabase CLI محلياً
supabase db reset
# أو
supabase migration up
```

### خطوة 2: تطبيق Migration على قاعدة البيانات البعيدة (Production)
1. اذهب إلى [Supabase Dashboard](https://app.supabase.com)
2. اختر مشروعك
3. اذهب إلى **SQL Editor**
4. انسخ محتوى ملف `supabase/migrations/add_restaurants_category.sql`
5. الصقه في SQL Editor
6. اضغط **Run** لتنفيذ الـ migration

أو استخدم Supabase CLI:
```bash
supabase db push
```

### خطوة 3: التحقق من الإضافة
```sql
SELECT * FROM categories WHERE id = 'restaurants';
```

يجب أن ترى:
- `id`: 'restaurants'
- `label`: 'مطاعم'
- `label_en`: 'Restaurants'
- `label_ur`: 'ریسٹورانٹس'
- `icon`: 'UtensilsCrossed'
- `emoji`: '🍽️'

## 📝 تفاصيل التصنيف

| الحقل | القيمة |
|------|--------|
| **ID** | `restaurants` |
| **العنوان (عربي)** | مطاعم |
| **العنوان (إنجليزي)** | Restaurants |
| **العنوان (أوردي)** | ریسٹورانٹس |
| **الأيقونة** | UtensilsCrossed |
| **الإيموجي** | 🍽️ |
| **الترتيب** | 130.5 (بين "طبخ منزلي" و "حلويات ومخبوزات") |

## 🎯 استخدام التصنيف

الآن يمكن للذكاء الاصطناعي تصنيف الطلبات المتعلقة بالمطاعم تلقائياً:
- "مطعم يمني" → **مطاعم**
- "مطعم مشاوي" → **مطاعم**
- "مطعم بيتزا" → **مطاعم**
- "مطعم فاخر" → **مطاعم**

## ⚠️ ملاحظات مهمة

1. **يجب تطبيق Migration على قاعدة البيانات** - بدون ذلك، لن يعمل التصنيف في قاعدة البيانات
2. **الترتيب**: تم وضع التصنيف بين "طبخ منزلي" (130) و "حلويات ومخبوزات" (131)
3. **ON CONFLICT**: إذا كان التصنيف موجوداً بالفعل، سيتم تحديثه بدلاً من إنشاء نسخة جديدة

## ✅ التحقق النهائي

بعد تطبيق Migration، تأكد من:
1. ✅ التصنيف يظهر في قائمة التصنيفات في الواجهة
2. ✅ الذكاء الاصطناعي يصنف الطلبات المتعلقة بالمطاعم بشكل صحيح
3. ✅ التصنيف يعمل في قاعدة البيانات (يمكن ربطه بالطلبات)

