# ⚡ اختبار سريع - خطوة بخطوة

## 🎯 الهدف
التحقق السريع من أن كل شيء يعمل بعد تطبيق إصلاحات الأمان.

---

## 📋 الخطوات (5 دقائق)

### **1️⃣ افتح Supabase SQL Editor**
- اذهب إلى [Supabase Dashboard](https://app.supabase.com)
- اختر مشروعك
- اضغط **SQL Editor** → **New Query**

---

### **2️⃣ شغّل ملف الاختبار**
انسخ والصق محتوى `TEST_FUNCTIONS.sql` واضغط **Run**

**النتيجة المتوقعة:**
- ✅ يجب أن ترى قوائم بالـ Functions والـ Triggers والـ Policies
- ❌ إذا ظهرت أخطاء = راجع رسالة الخطأ

---

### **3️⃣ تحقق من Security Advisor**
- اذهب إلى **Database** → **Security Advisor**
- يجب أن تختفي (أو تقل) التحذيرات المتعلقة بـ `function_search_path_mutable`

**✅ إذا اختفت = نجح**
**❌ إذا بقيت = شغّل `FIX_SECURITY_WARNINGS.sql` مرة أخرى**

---

### **4️⃣ فعّل Realtime (إذا لم يكن مفعّل)**
انسخ والصق محتوى `ENABLE_REALTIME_ALL.sql` واضغط **Run**

**النتيجة المتوقعة:**
```
pubname            | tablename
-------------------|----------
supabase_realtime  | conversations
supabase_realtime  | messages
supabase_realtime  | notifications
supabase_realtime  | request_views
supabase_realtime  | requests
```

---

### **5️⃣ اختبر من التطبيق**

#### **اختبار 1: افتح أي طلب**
1. افتح التطبيق
2. اضغط على أي طلب
3. اذهب إلى **Supabase Dashboard** → **Table Editor** → **request_views**
4. يجب أن ترى سجل جديد

**✅ إذا ظهر = نجح**

---

#### **اختبار 2: مرّر لأسفل في الطلب**
1. في نفس الطلب، مرّر لأسفل حتى 50% من المحتوى
2. اذهب إلى **request_views** مرة أخرى
3. يجب أن ترى `is_read = true`

**✅ إذا تم التحديث = نجح**

---

#### **اختبار 3: افتح "اهتماماتي"**
1. في التطبيق، اضغط على **اهتماماتي**
2. يجب أن ترى رقم بجانب الزر (عدد الطلبات غير المقروءة)

**✅ إذا ظهر الرقم = نجح**

---

## ✅ النجاح!

إذا أكملت جميع الخطوات بنجاح، فأنت جاهز! 🎉

---

## 🐛 إذا واجهت مشكلة

1. افتح **Supabase Dashboard** → **Logs** → **Postgres Logs**
2. انسخ رسالة الخطأ
3. راجع الملفات التالية:
   - Functions: `FIX_SECURITY_WARNINGS.sql`
   - Realtime: `ENABLE_REALTIME_ALL.sql`
   - Triggers: `CREATE_FUNCTIONS_AND_TRIGGERS_V2.sql`

